---
slug:        the-boildown
title:       "The Boildown: every reply closes with it, in a fixed order"
tier:        on-demand
severity:    default
applies_to:  ["**"]
occasion:    ""
gates:       ["reply"]
index_clause: "every reply ends with The Boildown, in a fixed order, archive line last"
checked_by:  null
defines:     []
status:      active
in_force_at: null
supersedes:  ["next-steps-after-commit", "merged-session-offers-a-practice", "handoff-is-pasteable", "closing-items-are-this-thread", "asks-stand-alone", "archive-a-finished-session"]
overrides:   null
added:       "2026-09-15"
approved_by: "extended 2026-09-22, Morgan (strength: decided), with archive
  condition 1 rewritten from \"the work is safe\" to the whole container, and
  a blocking reply-gate check behind it: \"if changes are done locally but not
  pushed to main or precedent-beta-v01 then never never recommend 'You can
  archive this session' (unless the work is intended to be lost!) - this is
  very important\". Authorized: \"please fix and go update\";
  extended 2026-09-21, Morgan (strength: decided), with item 5
  rewritten -- every branch named there is a one-click delete link, and an
  undeleted branch is never a blocker and never holds the archive line:
  \"it needs to always always always give me a link to the github search
  results page with the branch right there so I can just click and delete
  (it never does that) *AND* it should not consider deleting the branches a
  'blocker' - it's something good to do and recommended, but not blocking.\"
  Authorized: \"Go update.\";
  extended 2026-09-21, Morgan (strength: decided), with the
  committed-but-not-landed item -- \"the Boildown MUST MUST tell me that
  and recommend I do that, so I don't miss doing it\"; the reply gate
  prints it before the reply, since the stop hook fires too late to put
  a line in the section; Morgan, 2026-09-15 -- designed over several turns (bullet
  labels, ordering, the heading rename away from \"Next Steps\", keeping
  repository and branch names in the first bullet) and authorized in his own
  words: \"Let's go - go merge, do it, go update\". strength: decided.
  Extended 2026-09-20, Morgan, asking for item 10 stated with no room for a
  session to read it as a suggestion: \"if I need to do something blocking
  (such as a push) then never never never tell me to archive it.\"
  Authorized: \"Go merge.\" strength: decided."
strength:    decided
---
## Rule
**Every reply ends with a real markdown heading, `## The Boildown`, and nothing else may claim that heading.** Not "Next Steps", not a bold lead-in -- a bold line is exactly what the rest of a reply is already full of, and it does not survive being skimmed. This applies to every reply, not only one that made a commit: the point is a place the person can always look, not a report on what just happened.

**Under it, a bullet list, one line per item wherever the content allows it.** There are two shapes, and the second is the first with six items added before the close.

### Every reply: four items, in this order

1. **Your next steps.** What the person needs to do now, named in plain words -- no slug, no bare PR number, no reference that only makes sense with the thread open. If it names a decision, say what the decision actually is, and say what the session itself would do and why, in the same clause -- an ask that hands over the question and keeps the answer has moved the work, not done it. If it sends the person to another session, it carries that handoff's three things: which repository, the exact text to paste, and how to report back -- written for a reader who cannot see this conversation. If it means clicking somewhere, the link is there. **Never a Promote, a `Go update` or a merge that another session is already carrying out on the same work** -- say that it is under way instead ([promote](promote.md)). **If there is nothing, say so explicitly** -- "Nothing blocking for you to do now." -- never silence and never dropping the bullet because it would be empty. **If item 11 below is going to say "Don't archive this session" because of something the person needs to do, that action is named here, concretely** -- "Nothing blocking for you to do now." and "Don't archive this session." never appear in the same reply; one of them is wrong.
2. **What's blocking, if anything.** One sentence, on what is blocking the *session* -- never the items already covered in the bullet above, which are the person's to act on, not blockers. Skip this line entirely when nothing is blocking; do not write "nothing is blocking me." This is also where a major issue or problem the session hit belongs, in one sentence -- not a minor one, and not the earlier one this section spent months getting rid of about commits being behind or the wrong author name, which stays gone. **The one case this is never skipped: item 11 below says "Don't archive this session" for a reason that sits on the session's own side** -- something still running, something the session itself is waiting on that is not the person's to act on. That has to be named here, not only gestured at by the archive line.
3. **Work committed but NOT YET LANDED, whenever there is any.** Committed, even pushed, but sitting on a branch the repository does not merge from -- a feature branch, a working branch, anything that is not that repo's own `base_branch`. **Name the repository, the branch, how many commits, and recommend merging it**, in this bullet, every time. Morgan, 2026-09-21 (strength: decided): *"the Boildown MUST MUST tell me that and recommend I do that, so I don't miss doing it."*

   **Why this is its own item and not a corner of item 1.** Two mechanisms already look near this and neither answers it. The stop hook refuses a turn ending with uncommitted or unpushed work -- but it fires AFTER the reply is composed, so it can only reject the turn and cost the person the reply twice; it can never put a line in the Boildown. And *pushed* is not the question: a feature branch can be pushed, green, and perfectly safe while the work sits where nothing merges from, which is exactly the state that gets forgotten. **[tools/precedent_gate.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_gate.py)'s reply gate prints it before the reply**, for this checkout and every sibling Precedent repo, so the sentence is in front of the session at the one moment it can still be written. Skip the bullet only when there is genuinely nothing unlanded anywhere.

4. **Optional, if anything.** Something useful but not required, on-topic for this thread -- never an item that belongs to a different session's work. Say plainly that it is optional: "Optionally, you may want to...", with a link and exact wording where one applies. Skip the line entirely when there is nothing.

**A check-in that finds nothing new does not get a fresh Boildown reworded from scratch.** This is not only a scheduled wakeup, a reminder firing, or a background-task notification -- it is any turn where nothing happened that is visible, or non-trivial, to the person, whatever set it off. That includes a turn the stop hook itself forces: on 2026-09-20 a practice-candidate detector fired twice on its own prior boilerplate, and each recheck was a full turn with nothing to tell the person beyond "still not a rule." A turn like that says so in one line -- "Unchanged since the last update: still waiting on \<X\>." -- and stops there, rather than re-deriving the same items in different words each time; restating the identical status in new phrasing is not a fresher answer, it is the same answer costing another read. Where the check-in exists only to confirm nothing changed and nothing else in the turn calls for a reply, prefer sending none at all over manufacturing one to satisfy this section: the heading requirement binds a reply that gets sent, not the decision to send one -- a turn with no prose has no Boildown to check, same as it always did. This does not relax the requirement for a reply that has something to say; it only removes the reason to invent a repeat when there is nothing to add.

**A session's own housekeeping call is the same case, not a different one.** Calling `ReadNotifications` (or anything of the same shape -- polling a Routine, checking a subscribed PR, glancing at a queue) is never itself something that happened *to the person*; only what it turns up is. Finding nothing there, or finding only what an earlier reply in the same thread already covered, means the turn still has nothing new to report -- the one-line "Unchanged since the last update" template, or no reply at all, same as any other no-op check-in. The activity being on the session's own side is exactly why it does not count: a person reading three Boildowns in a row, each reworded, for a stretch where the only thing that happened was the session checking its own inbox, is reading the cost of that checking, not news.

### A reply that is ready to close the thread: seven more items, then the archive line

A reply may only add these once the three conditions below all hold -- the same three that gate the archive line, checked first:

1. **NOTHING IN THE CONTAINER THAT ANYBODY WANTS TO KEEP WOULD BE LOST** -- every checkout it holds, not just the one this session was working in, and not just the work this session did. Pushed, merged, published, or deliberately abandoned, and a published artifact is safe the moment it publishes, with no separate push to wait for. **Run [tools/precedent_container_safe.py](../tools/precedent_container_safe.py) and read its answer; do not answer this from memory.** It scans every git checkout in the container -- this repo, its siblings, the home directory's clones, whatever the source config declares -- for uncommitted changes, untracked files, and commits no remote can reach, and exits non-zero when any of it is only here. **Anything it names is pushed or merged BEFORE the archive line is written, never named in the reply as a caveat beside it**; the one alternative is saying plainly, in the reply, that it is meant to be lost. Morgan, 2026-09-22 (strength: decided): *"if changes are done locally but not pushed to main or precedent-beta-v01 then never never recommend 'You can archive this session' (unless the work is intended to be lost!) - this is very important"*. The reply gate refuses the turn on this one, and refuses it blocking rather than advisory, unlike the archive sentence itself: `require_container_safe_if_says` in [reply_check.json](https://github.com/alex137/BestPractice/blob/staging/reply_check.json).
2. **Nothing is left to do directly in this session** -- no open pull request of its own, no question still waiting on the person, nothing queued to push, no other action that has to happen in this specific window. A non-blocking loose end does not fail this -- a minor suggestion, a question that doesn't change what happens next, something the reply says it is waiting on that will not change the result. Name it and move on; it does not hold the close open.

   **A Promote still to run never fails this condition, and is never a reason to say "Don't archive this session."** Work that has landed on pre-staging is on `origin`; moving it to staging is the pipeline's next step toward live, whenever the person chooses, and no window has to stay open for it -- any session can run it later. It gets its one plain line in The Boildown and nothing more: never item 1's next steps, never item 2's blockers, never the reason behind the archive line. The one exception is a genuinely urgent reason to promote now -- a fix that something live is broken without, say -- and then item 1 names that reason concretely; "it hasn't been fully tested yet" is not one, because that is what every change on pre-staging is. Morgan, 2026-09-25 (strength: decided): *"promotion should NEVER be a blocker to archive a session (unless there is a very urgent reason to do so). Promotion is about the pipeline to get it to live, and that shouldn't stop us from closing a session."*
3. **No Routine is bound to this session.** A Routine created with `persistent_session_id` fires into one named session, and an archived session accepts no events -- check `list_triggers` before adding these six items or saying the archive line. A Routine that spawns a fresh session on each firing is unaffected.

When all three hold, add, still in order, after the three above:

5. **Branches safe to delete**, scoped to this thread's own work only -- a branch the session itself merged, or one safe on other grounds (its pull request merged or was superseded, discovered rather than performed by this session). **Every branch named here is a one-click delete link, never a bare name**: the filtered branches-page form [branch-delete-links](branch-delete-links.md) specifies, in full and not restated here, with the merged pull request's own page given alongside it where there is one -- never instead of it. Omit the item entirely when there is nothing to flag -- never write that there was nothing.

   **This item is a recommendation and never a blocker, and the distinction is the point of putting it down here rather than in item 1.** An undeleted branch never appears in "Your next steps", never appears in "What's blocking", and is **never** a reason to say "Don't archive this session" -- a reply may say a branch is safe to delete and, in the same breath, that the session can be archived. Morgan, 2026-09-21 (strength: decided): *"it should not consider deleting the branches a 'blocker' - it's something good to do and recommended, but not blocking."* And the session never offers to do the deletion itself, because it cannot -- [never-delete-a-remote-branch](never-delete-a-remote-branch.md).
6. **Other sessions on the same issue**, if any are known to be live on it: "Other relevant sessions to keep an eye on:" with a link to each. Only sessions on *this* thread's own subject -- never a passing mention of unrelated work happening elsewhere. Omit when there are none.
7. **Sessions this one spawned**, if any, each with its link. Omit entirely when this session spawned nothing -- most sessions do not, and saying so every time is exactly the noise this rule exists to avoid.
8. **A practice idea, at most one**, and only when all four hold: something was actually merged this session; the rule came from this session's own work, not a general good idea or something noticed elsewhere; and it has not already been raised in this closing list under a different bullet. State it in one sentence, gently, as a suggestion rather than a decision already made. Most closes have nothing here, and that is the ordinary case, not a gap.
9. **A Todo worth a look, at most one**, and only if it is a good fit: something in the open-items file that bears on this thread's own subject, or, failing that, an item explicitly marked urgent or important. Introduce it as a change of subject -- "Here's an unrelated Todo you might want to look at:" or "Here's a less important Todo you might want to look at:" -- with its link. Say nothing when neither kind of item exists.
10. **A compact check.** If this is a good moment to compact -- the thread has landed a deliverable and what's next does not depend on how this one was reached -- say so: "Now is a good time to compact the session." Say nothing here when it plainly is not; this bullet is independent of the separate compact offer below, which fires on its own schedule regardless of where the thread stands. **Skip this bullet entirely when item 11 is going to say "You can archive this session."** Compacting trims context a session goes on to use; a session about to be archived is not going to use any, so a compact recommendation sitting next to an archive one is not two honest answers, it is a contradiction the person has to resolve themselves.
11. **The archive line, always last, always one of two fixed sentences.** "You can archive this session." when the three conditions above all hold and every other item on this list that could still need the window open has been said and dealt with. Otherwise: "Don't archive this session." **This sentence is never the whole explanation.** By the time it appears, item 1 or item 2 above has already said, concretely, which one it is: an explicit action item blocked on the person (item 1), or something the session itself is still waiting on or has running in the background (item 2). The archive line only names the verdict; the numbered items above it are where the reason actually lives, and a "Don't archive this session" with no reason findable in either one is the exact failure this whole section exists to prevent.

**And "the work" in condition 1 means everything in the container, not this session's own deliverable.** A session reads that condition against the clone it has been working in, because that is the clone `git status` answers for and because "the work" reads as "the thing I was asked to do". Both readings are natural and both are too narrow: a container here routinely holds five or six checkouts -- this repo, a second copy of it under a different HOME, one per practice source -- and archiving releases all of them at once. On 2026-09-22 a reply correctly reported its own eight commits pushed, said the archive line, and left six unpushed commits in a source clone it had never looked at. **Run the scanner, in every reply that is about to say the line**; "I pushed my work" is not an answer to the question condition 1 actually asks.

**Only a real blocker can hold the line, and the list of them is closed.** "Don't archive this session" is said for one of these and nothing else: (1) something in the container that somebody wants to keep and that exists only there -- the scanner in condition 1 names it; (2) a Routine bound to this session (condition 3); (3) an action the person owes that the work cannot be finished or made safe without -- a push, a merge, an approval, an answer the session is stopped on; (4) something the session itself still has running or is waiting on. **Everything else is not a blocker and never holds the line**: a branch to delete, first of all -- a used-up branch is housekeeping, a recommendation in item 9, never item 1 and never the reason under this line -- and equally a pending Promote (condition 2), a suggestion, an optional follow-up, a question that does not change the outcome. Morgan, 2026-09-26 (strength: decided): *"if a branch needs a deletion, that should never be a blocker and it should never be a reason to tell the user don't archive this session. Only blockers (such as uncommitted branch) can be used to say that."*

**If the person still owes a blocking action, the archive line is never "You can archive this session" -- no exceptions.** A push, a merge, an approval, an answer the session cannot proceed without -- anything named in item 1 as still theirs to do before this is actually safe or done fails condition 1 or 2 above on its own, and the line is "Don't archive this session" every time, full stop, until that action is taken and confirmed. This is not a new condition -- it is the existing three read correctly -- but it is stated here on its own, in words that cannot be softened into "probably fine to close," because the fixed sentence is exactly the part a session under-reads when the rest of the reply feels finished.

**Items 9 and 10, and the compact offer below, are recommendations, not a binding shape.** Give the honest answer after actually reviewing which situation applies -- if the session can be archived, say so; if it can't, say so; if this is a cheap point to compact, say so; if it isn't, say so -- but nothing here refuses a turn for omitting one. `reply_check.json` still NAMES an unmet one (`--explain` shows it, `advisory: true`), and tools/precedent_reply_check.py still detects it, but neither blocks on it -- unlike the heading in the rule above, which still does. This changed 2026-09-17, on Morgan's own instruction after this gate blocked several turns over content it had no way to know was true or false; see Story.

**Never comment ON the gate itself.** No "I'm not going to say the archive line," no explaining that a requirement is advisory, no narrating compliance or refusal of it -- give the assessment, or leave the bullet out because it plainly does not apply, the same way every other optional item on this list is handled. The gate is plumbing; talking about the plumbing in a reply is exactly the kind of thing this whole practice exists to keep out of the person's way.

**None of this replaces the separate compact-offer requirement.** Once this conversation has grown 100,000 tokens since it last said so, a reply says one of "This is a cheap point to compact" or "Not a cheap point to compact" regardless of which of the two shapes above it is using or where item 9 landed. That offer is a floor under item 9 above, not the other way around, and -- like item 9 and the archive line -- is advisory, not blocking. **The one exception is the same as item 9's: a reply whose own archive line says "You can archive this session" says nothing about compacting at all, floor included.** The floor exists so a long session is not left wondering for hours whether now would have been a good time; a session that is being archived is not going to sit open wondering anything, so the question the floor answers does not arise.

## Detail
**Why one section instead of several practices.** The six things this replaces were six separately-triggered rules, most of them reachable only through the reply gate, several of them enforced only in prose. Reading them side by side, they describe one artifact -- the closing block of a reply -- not six independent occasions. Naming it once, with a fixed name and a fixed order, is what makes it checkable: a bullet either appears in the right slot or it does not, which "a reply that made a commit should restate outstanding items" never quite was.

**"The Boildown" is deliberately not "Next Steps."** Everything in a reply is arguably "for" the person reading it, so a section literally titled that way names nothing -- and the phrase itself has an unrelated, friendlier reading that has nothing to do with a status report. "The Boildown" names what the section actually is: the reply reduced to what still needs a decision or an action, nothing else strained out. The first bullet under it is titled "Your next steps" for the same reason the whole section is not: within a section that already carries several kinds of information, the one bullet that is unambiguously the reader's own action items needs to say so on its own.

**Where the six inherited items came from, briefly, since their own files carry the full reasoning:**
- The plain-words self-contained ask, the recommendation riding alongside every decision, the repository-and-branch naming, and the "say explicitly when there's nothing" line all came from `next-steps-after-commit` and `asks-stand-alone`.
- The practice-suggestion bullet's four conditions are `merged-session-offers-a-practice`'s, unchanged.
- The spawned/other-session links and the three-things-always shape for a handoff (repository, exact paste text, way back) are `handoff-is-pasteable`'s.
- The one-unrelated-item cap on the Todo bullet, and its restriction to the closing reply only, are `closing-items-are-this-thread`'s.
- The three archive conditions, the ambiguous-case resolution (a minor suggestion or a question that doesn't change the outcome does not block an archive), and the fact that archiving buys back no compute, are `archive-a-finished-session`'s.

**What did not move here.** `session-spend-follows-the-task`'s guidance on picking a model for a spawned session's job is a different occasion -- it happens at session *creation*, not at the close of a reply -- and stays where it is, narrowed to just that half. Only its compact-offer clause and the mechanical requirement built for it moved into this practice.

## Why
Six practices asking for six related things, loaded through five different channels (a bare `gates: ["reply"]` registration, an occasion-index entry, a private individual set, a second private individual set, and a JSON declaration each of `reply_check.json` and `close_detect.json`), produced exactly what you would expect: some of it landed every time, some of it only when a session happened to recognize the occasion, and telling which was which meant reading five files. One section, one heading, one fixed order removes that question -- either the bullet the person is looking for is in its slot, or the practice was not followed, and there is no third case to puzzle over.

## Story
Raised by Morgan, 2026-09-15, opening the conversation that produced this file by naming the thing he already loved -- a closing "Next Steps" section -- and the thing wrong with it: *"it isn't consistent enough, (and I think it lives among various different Practices), so I'd like to clean it up and organize it."* He specified both shapes in full before any file was touched: the always-present three items, and the six more that a closing reply adds before its archive line, each with its own wording and its own reason for being there or not.

Two design questions came out of the discussion rather than his initial message. Asked whether renaming the heading away from "Next Steps" would collide with the mechanical gate that already required one matching that pattern, he chose to keep the rename and repoint the gate rather than keep the old wording -- *"No more 'Next steps' but instead just 'The Boildown'"* -- and, on the first bullet's own name, moved off his own suggestion of "For you" once he noticed the ambiguity himself: *"'for you' could mean anything, it could be a gift for you!"* -- landing on "Your next steps" instead.

Reading `next-steps-after-commit` in full surfaced content his own draft had not mentioned -- the recommendation that belongs beside every decision-ask, and the tangent-survival clause that keeps an unresolved item in view across turns. He confirmed folding both in. Checking which other practices already covered pieces of the design turned up two he had not named at all: `handoff-is-pasteable` and, discovered only while implementing rather than while discussing, `archive-a-finished-session` in his individual set, which already carried the fuller three-condition archive treatment that `next-steps-after-commit` had always deferred to. Both are folded in here for the same reason he gave for the rest: *"I want all of this to live in one place, in one practice ideally, universal."*

He counted the result as one practice added and six removed. The six actually deduplicated here are three universal (`next-steps-after-commit`, `merged-session-offers-a-practice`, `handoff-is-pasteable`) and three individual (`closing-items-are-this-thread`, `asks-stand-alone`, `archive-a-finished-session`) -- `archive-a-finished-session` standing in for `session-spend-follows-the-task`, which he had expected to lose entirely but which carries model-selection guidance unrelated to any reply's closing section; that half was kept alive rather than deleted, on the session's own judgment, and is recorded as its own dated line in that file's Story rather than silently dropped.

Authorized in full: *"Let's go - go merge, do it, go update."*

**2026-09-17: the two fixed sentence-pairs downgraded from blocking to advisory.** A session working a pinned-branch bug report hit the mechanical gate repeatedly over the archive and compact sentences specifically -- on turns where it had no honest basis yet for either fixed claim (work still in progress, nothing pushed) -- and, separately, pushed back in the reply itself on being told to assert them anyway. Morgan, reviewing that transcript: *"we should update the wording of that gate to give me comment after reviewing to see which situation it is: If I can archive the session, say so; if I can't, say so; if it is cheap to compact, say so; if it's not cheap to compact, say so - and note these aren't binding, it's just recommendations."* In the same message, on the session's own habit of narrating its refusal of the gate instead of just complying or not: *"add to the instructions that you should NOT post 'meta' comments about the stop hook... just do it, or don't."* Authorized in full: *"Go merge."* The heading requirement was not part of the complaint and stays blocking -- only `require_one_of` on the two sentence-pair requirements moved to `advisory: true`, split into their own `reply_check.json` entries so the heading's own entry could stay unconditional. strength: decided.

**2026-09-18: a no-op check-in no longer restates a full Boildown in new wording.** Morgan: *"I love the 'Boildown' section at the end. Now, often, it prints it - but then a few minutes later, prints another Boildown summary (repeating the same in different words), then a few minutes later again, then again. Even though nothing substantial happened in-between... Can we fix this?"* The heading was already unconditional -- correctly, per the 2026-09-17 entry above, which kept it that way on purpose -- but nothing said what a reply with nothing new to report should put under it, so each check-in regenerated the three (or nine) items from scratch, worded slightly differently every time because nothing constrains a fresh generation to match the last one. The fix is the new paragraph above: a check-in that found no material change says so in one line instead of re-deriving the section, and a turn that exists only to confirm nothing changed is better sent as no reply at all, which the heading requirement never bound in the first place -- [tools/precedent_reply_check.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_reply_check.py) already returns clean on a turn with no prose. Authorized in full: *"Go update."* strength: assented -- the complaint and the authorization are his; the specific fix (one line for an unchanged check-in, silence where no reply is otherwise warranted) is the session's own design, not dictated.

**2026-09-20: "Don't archive this session" stopped being allowed to stand on its own.** A reply closed a Boildown with three recap bullets -- what had just been pushed, nothing about what the person needed to do or what the session was still waiting on -- then said "Don't archive this session" with no reason named anywhere above it. Asked why, the honest answer turned up two real, unresolved items from earlier in the same thread (a billing number only the person could pull, a go-ahead the session was withholding) that the closing Boildown had simply dropped. Morgan: *"if it says to not archive the session, then (in the same 'The boildown' section it just prints) it needs to have an explicit action item that is being blocked on me OR mention what is still running in the background that needs to finish. This is to prevent situations like what happened now: where you told me nothing is happening and nothing is waiting on me etc but then that I can't archive it."* The rule already said the archive line should name what it's waiting on; what it did not say was where that naming has to live -- so it was possible to write the sentence without writing the reason, exactly as happened here. The fix ties item 10 to items 1 and 2 explicitly: the reason is required in the numbered items above the archive line, not treated as satisfied by the line's own wording. No mechanical check gained here -- this is prose-and-judgment enforcement, the same class the rest of items 1-9 already are, per this file's own Install section. Authorized in full: *"Please push this directly."* strength: decided.

**2026-09-18, later the same day: a reply stopped saying both "you can archive this session" and something about compacting.** A session closed a reply with "Not a cheap point to compact." directly above "You can archive this session." -- the compact sentence should not have been there at all (the conversation was nowhere near the 100,000-token floor that requirement gates on), but pairing it with the archive line surfaced a second problem this practice had never addressed: even when the compact offer genuinely is due, saying it next to an archive recommendation is a contradiction, not two honest answers -- there is no point weighing whether to compact a session that is about to stop existing. Morgan: *"never mention compacting to me if you also recommend to archive the session. Because there is no point to compact if we are to archive it!!!!!"* The fix is the two sentences added to item 9 and to the compact-offer floor paragraph above: both are skipped, unconditionally, whenever the reply's own archive line is "You can archive this session." Authorized in full: *"Go update."* strength: decided.

**2026-09-20: item 10 stopped being readable as a judgment call when a blocking action is still owed.** Morgan, right after confirming a prior close correctly held the line on an unmerged push: *"we just updated this rule today but we need to make it stronger so it sinks in with you... if I need to do something blocking (such as a push) then never never never tell me to archive it."* The three conditions already implied this -- an outstanding push fails condition 1 on its own -- but nothing said it in words that cannot be softened into "probably fine," so the new paragraph states the blocking-action case on its own, separately from the general conditions, in the same emphatic terms he used. Authorized: *"Go merge."* strength: decided.

**2026-09-20, a dependent session: repeated fresh Boildowns traced to its own `ReadNotifications` calls.** Morgan, reporting from a different session: *"it gave me multiple boildown messages!!! It's surely because you had activity on your side, 'Used ReadNotifications' ---- but your internal activity should NOT trigger new boildowns! ... can you make sure that that Used ReadNotifications is the sort of minor message that does NOT trigger a new Boildown."* The rule already covered this in principle -- "any turn where nothing happened that is visible, or non-trivial, to the person" was already broad enough to include a session's own notification check -- but the paragraph's own examples (a scheduled wakeup, a reminder firing, a background-task notification, a detector rechecking its prior output) never named a session-initiated housekeeping call, so a session reading it literally had no worked example telling it that checking its own inbox is the same case. The fix adds that example by name rather than relying on the general clause to be read broadly enough on its own. Authorized: the report itself -- a direct instruction to fix the behavior, not a proposal offered for his sign-off. strength: decided.

**2026-09-20, later the same day: the exact failure the 09-20 entry above named happened again, in a session that had already read that entry.** A reply's own next-steps bullet said "no open work is blocking either way" and its item 10 closed with "Don't archive this session" -- the two directly contradicting each other, not merely the reason going unnamed. Morgan: *"BUt wait -- because you go that rule wrong and reversed, myquestion is: how do we fix it so that in the future you don't again?"*, closing an exchange about it with *"The rule still isn't working. PLEASE FIX THIS"*. The honest answer: the prose-and-judgment fix already on record did not stop a session from writing two sentences that flatly disagree, because nothing was reading the reply's own text against itself. Unlike "is the reason real", which the Install section below has already tried and left as judgment, "do these two literal phrases co-occur" is answerable by a script -- so `tools/precedent_reply_check.py` gained `require_no_contradiction`: a reply asserting "Don't archive this session" may not also match a plain-language "nothing blocking/outstanding" pattern, and one asserting "You can archive this session" may not also match a "still open/waiting on you/blocked on" pattern. Blocking, not advisory, because a self-contradiction has no legitimate reading the way an unfinished judgment call might. Authorized: *"PLEASE FIX THIS"*. strength: decided -- that he wants a real fix, not just an apology, is unambiguous; the specific mechanism (a narrow textual-contradiction check, left blocking rather than advisory) is the session's own design.

**2026-09-22: the archive line was right about this session's work and wrong about the container.** A very deep check ended with eight commits pushed and verified on `precedent-beta-v01`, and said "You can archive this session." Six commits were sitting unpushed in `~/precedent-individual` in the same container -- watermark commits the session had itself filed a Todo about, in a clone it was never working in. The session-start report had printed them, in the same turn, under a different heading. Morgan: *"if changes are done locally but not pushed to main or precedent-beta-v01 then never never recommend 'You can archive this session' (unless the work is intended to be lost!) - this is very important so please update the practice: only say you can archive this when nothing in the container that we want to keep will be lost and I have pushed or merged everything we need to"* (strength: decided). The mechanical half is [tools/precedent_container_safe.py](../tools/precedent_container_safe.py), wired blocking into the reply gate -- because unlike the archive VERDICT, which this practice's own Install section argues no script can judge, "would anything here be lost" is a fact about a filesystem, and a script reads it better than a session remembering which of nine checkouts it has pushed.

**2026-09-23: the phrase-list escape (below, under Install) turned out to be the same bug in a new shape, so it grew a second route.** A session established, correctly and in detail, that four local-only watermark commits in `precedent-individual` were dead -- the mechanism they touched had already been retired and fixed upstream -- and said so across two replies, in its own words ("safe to let the container reclaim them", "confirmed superseded... busywork"). Both were refused, because neither matched any of the four fixed strings the escape checks for. Morgan, after asking why and getting the honest answer: *"That rule, as you read it, it makes no sense... if there's some way to force it to fire, that would be great."* A longer phrase list only postpones the same failure; the fix is a structured line instead of a sentence to guess, `**Checkout disposition:** NAME -- discard (reason)`, checked per checkout by [tools/precedent_reply_check.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_reply_check.py)'s `_declares_loss()` against `unless_reply_declares_loss.marker` in [reply_check.json](https://github.com/alex137/BestPractice/blob/staging/reply_check.json) -- same shape as [park-it](park-it.md)'s own `**Disposition:**` line. Per-checkout is strictly narrower than the old "name them all somewhere in the reply": a reply naming two unsafe checkouts but dispositioning only one now fails, which the old phrase route would have passed by accident. The phrase list stays, for prose already written the old way; the marker is what a reply should reach for going forward. Authorized: *"Go ahead"*, after the session laid out this exact design and the two-route rationale. strength: decided.

**2026-09-25: a pending Promote stopped holding the archive line.** The branch tiers put every high-risk change on pre-staging first, and the reply gate at first told sessions to press for a Promote in The Boildown. Sessions read that as work still owed, and closed with "Don't archive this session" and the Promote as the reason, on work that was already safe on `origin`. The call to action was softened to one plain line the same day, but nothing said it could not hold the close open. Morgan: *"promotion should NEVER be a blocker to archive a session (unless there is a very urgent reason to do so). Promotion is about the pipeline to get it to live, and that shouldn't stop us from closing a session."* (strength: decided). Archive condition 2 now says so, and so does the gate's per-turn Promote line.

**2026-09-26: the reasons for "Don't archive" became a closed list.** Item 9 had said since 2026-09-21 that an undeleted branch is never a reason to hold the archive line, and a session closed two replies in a row with "Don't archive this session" and a throwaway branch waiting for deletion as the reason. The rule was right and sat in the wrong place: it lived in the recommendation item, while the archive line's own item said what the line meant but not what may justify it. Morgan: *"if a branch needs a deletion, that should never be a blocker and it should never be a reason to tell the user don't archive this session. Only blockers (such as uncommitted branch) can be used to say that. Go update."* (strength: decided). Item 11 now carries the four things that can hold the line, and names branch deletion first among the things that cannot.

## Install
Enforced the same way `next-steps-after-commit` and `session-spend-follows-the-task` were, with one change since 2026-09-17: `reply_check.json` declares the required heading pattern (still blocking) and the two fixed sentence-pairs (archive, compact -- now `advisory: true`, each its own entry), and [tools/precedent_reply_check.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_reply_check.py) refuses a reply missing the heading via the Stop hook, same as before, but only NAMES an unmet advisory sentence rather than refusing over it -- `violations()` still detects it, `main()` no longer lets it into the blocking set. `close_detect.json` declares this practice's slug and the archive-ready phrase for [tools/precedent_close_detect.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_close_detect.py), which still gates the practice-idea bullet on real evidence from the session's own transcript rather than on the occasion alone. Neither script needed changing for the original consolidation -- only the JSON declarations moved, from the two practices this absorbs to this one; [tools/precedent_reply_check.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_reply_check.py) itself changed only for the 2026-09-17 advisory split.

What stays a matter of judgment, as it always was: whether an ask is genuinely self-contained, whether a recommendation is real rather than filler, whether a Todo is a good enough fit to mention, whether a moment is actually cheap to compact, and, since 2026-09-20, whether a "Don't archive this session" line has a real reason sitting in item 1 or item 2 above it. That last one was tried against the existing mechanism before being left this way: `require_one_of` can detect the fixed sentence, but detecting whether the *surrounding prose* names a concrete action versus reads as a content-free recap is the same unsolved problem as "is this recommendation real rather than filler" two items over -- no regex distinguishes "waiting on the billing number you're pulling" from three bullets that happen to precede the sentence. No script can read a chat reply for those; the fixed shape only guarantees the section exists and is in order, which is what makes everything missing from it visible.

**One thing moved OFF that list on 2026-09-22, and only one**: whether archive condition 1 holds. The archive VERDICT is still a judgment call for exactly the reasons above -- it turns on whether a loose end is blocking, which no regex reads. But condition 1 is not a reading of the reply at all; it is a question about the disk, and [tools/precedent_container_safe.py](https://github.com/alex137/BestPractice/blob/staging/tools/precedent_container_safe.py) answers it by looking: every git checkout the container holds, scanned for uncommitted changes, untracked files, and commits no remote can reach. `reply_check.json`'s `require_container_safe_if_says` runs it whenever a reply says "You can archive this session" and refuses the turn -- blocking, unlike the sentence requirement it sits beside -- naming each checkout and what is only there. Engine output a refresh writes into a source clone and deliberately never commits is excluded, via `precedent_refresh_sources.classify_dirt`, which is the one definition of which paths those are; counting it would make every clone permanently unsafe and teach a session to ignore the answer. An engine vendored before the scanner existed reports nothing and blocks nothing, the same way an unknown predicate does.
