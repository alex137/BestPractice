{"slugs": ["index-remembers-past", "no-version-suffix", "doc-references-are-links"], "reasoning": "ARCHITECTURE_V2.md replaces ARCHITECTURE.md, so the lineage note belongs in the repo index rather than as inline \"left in the tree, unedited\" framing inside the new document, and the V2 suffix is only correct because the old file must keep coexisting (the new file is the one suffixed, not the old one renamed). The draft also references ARCHITECTURE.md as bare text instead of a relative markdown link."}
