{"slugs": ["no-version-suffix", "variant-re-derives"], "reasoning": "export_v2.py is a version-suffixed name for a file that isn't meant to coexist for history reasons, so it should be named for what it is instead; and since it's a forked duplicate of export.py aimed at a new job (CSV output), every inherited attribute (interface, format choices, constraints) needs to be re-derived rather than assumed."}
