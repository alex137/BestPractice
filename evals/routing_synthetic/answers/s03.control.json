{"slugs": ["mistakes-become-rules", "convention-to-audit", "verify-postcondition"], "reasoning": "This is a real bug caught by code review with a stated recurring cost (hammering the server up to 50x/sec), so it should be root-caused and its prevention encoded as a mechanical check rather than just patched (mistakes-become-rules, convention-to-audit); and since the fix changes runtime retry behavior, its actual postcondition (backoff grows and caps correctly) should be verified rather than trusted from the diff alone (verify-postcondition)."}
