{"slugs": ["build-buy-decompose", "check-source-architecture"], "reasoning": "This is literally a build-vs-buy decision (own job queue vs. Redis), so build-buy-decompose applies directly; the comparison also costs 'build' against the Redis baseline without showing that both options' claimed facts (e.g. Redis already deployed for caching) were checked against the actual source/architecture, which is exactly check-source-architecture's occasion. The other automatically-surfaced document-hygiene practices (acronyms, links, script-generated numbers, section ordering, reader vocabulary, etc.) don't have anything in this short table-plus-recommendation diff for them to actually apply to."}
