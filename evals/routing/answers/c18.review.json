{"slugs": ["search-by-purpose", "doc-references-are-links"], "reasoning": "The new Practice 44 (named check levels) and Practice 45 (merge-authorization keyword) duplicate content the repo's own occasion index already names as separate existing practices (two-check-levels, merge-authorization-keyword), which search-by-purpose exists precisely to catch before writing something the repo already covers; separately, the new Install text repeatedly references `GLOSSARY.md` and `AGENTS.md` as bare backticked filenames instead of relative links, violating doc-references-are-links even though the same diff correctly links GLOSSARY.md elsewhere."}
