# Harness adapter family — transfer ledger

Practice: [parallel-artifact-ledger](../../practices/parallel-artifact-ledger.md).
The family: [claude-code/](claude-code/), [codex/](codex/), and
[gemini-cli/](gemini-cli/) are one design (the practice layer's harness
wiring) in three parallel forms — see [README.md](README.md) for what each
adapter actually is. A change to the mechanism inside one presumptively
transfers to the others; this table records, per change, the verdict for
each member — *applied as `<what>`*, or *no transfer because `<reason>`* —
so a headline-level "this one's harness-specific" call can't silently skip
a mechanism that should have propagated. Origin incident:
[parallel-artifact-ledger](../../practices/parallel-artifact-ledger.md)'s
own `## Story`. Add a row here in the same commit as any change to a
member's wiring, before this file existed retroactively for the one already
in the tree.

| Date | Originating change | claude-code | codex | gemini-cli |
|---|---|---|---|---|
| 2026-07-20 | [`f2078d6`](https://github.com/alex137/BestPractice/commit/f2078d6ef32731e35d30e279c90d72a55e9b6268) — the commit that created this family in the first place, replacing the old flat `CLAUDE.md.template`/`settings.json`/`session-start.sh` layout with `templates/harness/README.md` plus one adapter directory per harness | created together with codex and gemini-cli, as the family's own inception commit — `CLAUDE.md`, `hooks/session-start.sh`, `settings.json` — no transfer verdict applicable (nothing pre-existed for any member to transfer from or to; the three came into being as parallel siblings in the same commit) | created together, same reason as claude-code — `README.md` | created together, same reason as claude-code — `GEMINI.md` |
| 2026-08-29 | [`9de83a2`](https://github.com/alex137/BestPractice/commit/9de83a283735da32985142bbbb3e3239cf68737f) — Stop hook blocking a turn end with uncommitted/untracked/unpushed work | applied as `hooks/stop-git-check.sh`, wired into `settings.json`'s `Stop` hook | no transfer because codex has no teardown/stop-hook mechanism to wire it into (per [README.md](README.md)'s adapter table — a soft-guarantee harness, not a decision left open) | no transfer because gemini-cli has no teardown/stop-hook mechanism either, same reason as codex |
| 2026-09-01 | [`969be87`](https://github.com/alex137/BestPractice/commit/969be87c8ad9268795276febe7c597d274e515bd) — an accidental merge of Alex's own in-progress branches added scaffolding to all three adapters, then was reverted the same session as an authorization mistake unrelated to the content's merit | reverted; no transfer verdict applicable — the content never actually landed as this repo's own decision | reverted, same as claude-code | reverted, same as claude-code |
| 2026-09-03 | [`0980ae3`](https://github.com/alex137/BestPractice/commit/0980ae3bf051a97ac0fb5aecc70e7e6590fb0163) — wired the path-triggered loading channel into a `PreToolUse` hook | applied as `hooks/precedent-paths.sh`, wired into `settings.json`'s `PreToolUse` (matcher `Edit\|Write\|NotebookEdit`) | no transfer because codex has no hook mechanism to wire it into, same reason as the Stop hook rows above | no transfer, same reason as codex |
| 2026-09-04 | [`36dbeb9`](https://github.com/alex137/BestPractice/commit/36dbeb90e18ffa222c5fca117d00a23ae6feed43) — the gate-channel audit found `reply` cited as firing "at the stop hook" but never actually wired there | applied: `hooks/stop-git-check.sh` now also fires `tools/precedent_gate.py reply` before blocking on git hygiene | no transfer because codex has no teardown/stop-hook mechanism (same as the row above) — `reply` stays cited-only there, by design, not by omission (the commit's own stated reasoning) | no transfer, same reason as codex |
| 2026-09-05 | [`6e4d48f`](https://github.com/alex137/BestPractice/commit/6e4d48f3ae8e3a2b97bc0de31f940b09812c9f07) — the individual-source `SessionStart` hook race fix added a new, retry-capable canonical hook template | applied as `hooks/individual-source-bootstrap.sh.template`, instantiated per-adopter by `tools/precedent_bootstrap_source.py --write-session-hook` | no transfer because codex has no `SessionStart`-equivalent hook mechanism to wire a bootstrap script into, same reason as every other hook-based row above | no transfer, same reason as codex |
| 2026-09-06 | [`7fa0dc7`](https://github.com/alex137/BestPractice/commit/7fa0dc7b7ab636dbbe20da114a8654d067f33f82) — corrected the same hook template's header comment: a retry loop inside the `SessionStart` hook cannot close the `add_repo` access gap (a follow-up testing session proved the prior claim false) | applied: `hooks/individual-source-bootstrap.sh.template`'s header comment corrected in place; no mechanism change, no new file | no transfer because codex has no member file this correction touches, same reason as the row above | no transfer, same reason as codex |
| 2026-09-06 | [`7d4988d`](https://github.com/alex137/BestPractice/commit/7d4988dad5527ee0a436f245b04fe0362e1ea33c) — the pre-launch audit's new-install pass: the `PreToolUse` context hook stopped emitting a permission verdict, and the allowlist was repointed from the classic `process/upstream/tools/` paths (dead in a Precedent-loader install) to the loader's own commands, each listed bare AND with arguments | applied as `hooks/precedent-paths.sh` (dropped `"permissionDecision": "allow"`, keeping `additionalContext`) and `settings.json` (36 allowlist entries, both layouts) | no transfer because codex has neither a hook mechanism to carry the first change nor a permission-allowlist file to carry the second — same reason as every other hook-based row above | no transfer, same reason as codex |
| 2026-09-06 | [`fc90ea6`](https://github.com/alex137/BestPractice/commit/fc90ea6419dfb30f0361567c07356807eadcc5f8) — the `PreToolUse` context hook stopped re-sending Rules it had already sent this session, keying a scratch `--seen-file` on the session id | applied as `hooks/precedent-paths.sh` (reads `.session_id` off the hook payload, passes `--seen-file $TMPDIR/precedent-paths-seen-<id>.txt` to the vendored engine; the dedup logic itself is in `tools/precedent_paths.py`, per engine-plus-host-shims) | no transfer because codex has no hook mechanism to carry it — same reason as every other hook-based row above | no transfer, same reason as codex |
| 2026-09-06 | [`b0a5efe`](https://github.com/alex137/BestPractice/commit/b0a5efed4197ecd23486956ab4f95bc844996763) — the product rename to Precedent reached one member file, as a single word inside a shell comment | applied as `hooks/individual-source-bootstrap.sh.template`'s header comment; **no mechanism change** — the row exists because this ledger's own rule is that every commit touching a member carries a verdict, including "nothing to transfer" | no transfer because codex has no member file carrying this comment | no transfer, same reason as codex |
| 2026-09-06 | [`7642084`](https://github.com/alex137/BestPractice/commit/7642084be476a70483df905b99bfbcf1d1b629a8) — the Stop hook skips a repo with no remote at all, instead of holding the turn open forever against a remote-tracking ref that cannot exist | applied as `hooks/stop-git-check.sh`; found already fixed by hand in a consuming repo, so this is the abstracted form coming back upstream (`practice-export-loop`) rather than a change originating here | no transfer because codex has no teardown/stop-hook mechanism to wire it into — same reason as the original Stop-hook row above | no transfer, same reason as codex |
| 2026-09-06 | [`a347539`](https://github.com/alex137/BestPractice/commit/a347539554bfcc11394b2081c190bfdc8581797c) — the individual-source bootstrap hook stopped naming one person: the instantiated hook reads `PRECEDENT_INDIVIDUAL_REPO` and falls back to the account baked in at instantiation, so several people on one shared project each reach their own set without a second, conflicting hook | applied as `hooks/individual-source-bootstrap.sh.template` (one `REPO_URL` line before the exec; the `--name` is deliberately NOT overridable, since source-naming fixes it as the same string in every account) | no transfer because codex has no hook mechanism to carry it — same reason as every other hook-based row above | no transfer, same reason as codex |
| 2026-09-06 | [`810a1dc`](https://github.com/alex137/BestPractice/commit/810a1dc3c51062888c2ac1da70d47574da903550) — two session hooks: `freshness-guard.sh` (never work on, or write to, a stale checkout) and `commit-identity.sh` (a commit's author is the person running the session, not the container's own agent account) | applied as `hooks/freshness-guard.sh` and `hooks/commit-identity.sh`, wired into `settings.json` — the guard twice (`SessionStart`, and `PreToolUse` with matcher `Edit\|Write\|NotebookEdit\|Bash`), the identity hook once at `SessionStart` | no transfer because codex has no hook mechanism to wire either into — the same reason as the `PreToolUse` and Stop-hook rows above. The identity half is the first row here whose mechanism is not harness-specific at all (it is a shell script that only needs to be run once per session), so the absence is purely the missing invocation point, not a missing capability | no transfer, same reason as codex |
| 2026-09-06 | [`03f201c`](https://github.com/alex137/BestPractice/commit/03f201c691b0f08f14dd286468d07e66adef7cc6) — `commit-identity.sh`: a repository's own `identity.json` becomes a resolution step, and the `pre-commit` backstop enforces a DECLARED author (never an inferred one) | applied as `hooks/commit-identity.sh`; no wiring change — the same `SessionStart` entry from [`810a1dc`](https://github.com/alex137/BestPractice/commit/810a1dc3c51062888c2ac1da70d47574da903550) runs it | no transfer because codex has no hook mechanism to wire it into, same reason as that row | no transfer, same reason as codex |
| 2026-09-09 | [`67df89d`](https://github.com/alex137/BestPractice/commit/67df89d) — `commit-identity.sh`: the fallback timezone becomes America/Argentina/Buenos_Aires and is APPLIED (system clock repointed, `TZ` written into the harness `env` block) instead of skipped as a guess, and the zone is read from `precedent.json`'s `fallback_timezone` so a repo sets its own; the pre-commit backstop still enforces only a DECLARED zone | applied as `hooks/commit-identity.sh` (byte-identical to `.claude/hooks/commit-identity.sh`, as the harness's own copy check requires); no wiring change — the same `SessionStart` entry runs it | no transfer because codex has no hook mechanism to wire it into — the same reason as every other hook-based row above. Worth stating for this row specifically: the mechanism is NOT harness-specific (it is a shell script needing one invocation per session), so the absence is the missing invocation point, exactly as [`810a1dc`](https://github.com/alex137/BestPractice/commit/810a1dc3c51062888c2ac1da70d47574da903550)'s row recorded for the identity hook's arrival | no transfer, same reason as codex |
| 2026-09-10 | [`82572e7`](https://github.com/alex137/BestPractice/commit/82572e7d85f14cb0d906a6dd6b78a7360a4ea85f) — `individual-source-bootstrap.sh.template` stopped baking a full account URL into a file the consuming repo TRACKS, and derives it from `PRECEDENT_SOURCE_BASE_URL` plus the name [source-naming](../../practices/source-naming.md) already fixes (`precedent-individual`). INSTALL.md §8 already said no tracked file names the account owning the private sets; that was true of the three team sets and false of the individual one, five lines below this template's own comment saying a public consumer should bake in no URL | applied as `hooks/individual-source-bootstrap.sh.template` — the derivation, and both environment reads written `${VAR:-}` so the hook survives `set -u` on a machine that never set the variable, which is every machine the derivation exists to serve. No wiring change: the file is instantiated per-adopter by `tools/precedent_bootstrap_source.py`, not referenced from `settings.json`. The defect was shared exactly as widely as the mechanism — every repo instantiating this hook was baking in its own owner's account and republishing it | no transfer because codex has no hook mechanism to instantiate this template into — the same reason as every other hook-based row above. The reasoning is not adapter-specific, though: a codex user who sets the same base URL gets nothing from it, because there is no bootstrap hook to read it, which is the pre-existing adapter gap the 2026-09-07 `adc36bb` row already names | no transfer, same reason as codex |
| 2026-09-10 | [`abaa952`](https://github.com/alex137/BestPractice/commit/abaa952036fecb9d87e3305ff84aa8ebdb327bfb) — `individual-source-bootstrap.sh.template`: the "am I still a raw template?" guard tested `$REPO_URL` against the source-repo-url placeholder, which the substituter rewrites along with every other occurrence in the file, so every instantiated hook compared the real URL against itself and took the "no repository URL" exit on every session while holding the correct URL. Two mechanisms, decomposed per the practice's own rule: (a) the guard moves onto a separate substituted-sentinel placeholder; (b) a general rule that no comment in a template may spell a placeholder out, since substitution rewrites comments too and turned this fix's own explanation into nonsense | applied as `hooks/individual-source-bootstrap.sh.template`; no wiring change — `settings.json` never referenced this file (it is instantiated per-adopter by `tools/precedent_bootstrap_source.py --write-session-hook`, which supplies the new sentinel). Mechanism (b) is checked at the family level: `tools/verify_harness.py` now fails any comment line in that template carrying a `{{...}}` literal | no transfer because codex has no hook mechanism to instantiate this template into — the same reason as every other hook-based row above. Mechanism (b) would transfer to any codex file carrying substituted placeholders, and codex has none: its adapter is `README.md` plus an environment setup script, neither templated | no transfer, same reason as codex |

**Mechanically audited as of 2026-09-05**: `tools/precedent_check.py`'s
`parallel-artifact-ledger` check (`checked_by` on
[the practice file](../../practices/parallel-artifact-ledger.md)) walks
`git log --no-merges` for each member directory and fails if any commit's
hash isn't referenced somewhere above — the "audit that fails any change
date lacking a complete row" the practice's own Rule names. Found four
real backfill gaps on its first run (the three rows above added 2026-09-05,
plus the reply-gate row added 2026-09-04 the day before the audit existed)
— this file's own retroactive backfill from 2026-09-04 had missed them.
What it does not check: whether a recorded verdict is *correct*, only that
a row exists.

**Fifth gap found 2026-09-05, later the same day**, by a pull request (PR)
unrelated to this file whose continuous integration (CI) still failed
against it (`tools/precedent_check.py`'s
`--only parallel-artifact-ledger` fails against `templates/harness/`
regardless of what a given diff touches, since its scope is the whole
tree, not the change): the `f2078d6` row above, the family's own
inception commit, had been missed by every prior backfill pass —
including the 2026-09-05 one this note originally described as
exhaustive. The check's own exemption only excludes the *repository's*
root commit (`git rev-list --max-parents=0`), not each family's own
first commit; `f2078d6` predates the ledger file itself by five weeks
and was never a repository root, so nothing had excluded it. Backfilled
by row instead of by changing the check's exemption logic — whether to
also exempt each family's own inception commit mechanically is tracked as
[TODO.md's `ledger-root-commit-exemption` item](../../TODO.md#ledger-root-commit-exemption), a separate, smaller follow-up not
folded into this fix.
| 2026-09-06 | [`0e1ab9d`](https://github.com/alex137/BestPractice/commit/0e1ab9d5911a3270770dc8aac669d905c65b929f) — freshness is rechecked on an idle return, not only at session start and first write: `freshness-guard.sh` gains a throttled `user-prompt` mode | applied as `hooks/freshness-guard.sh` (new `user-prompt` mode, mtime-throttled, delegating to the existing session-start checks) and `settings.json` (a `UserPromptSubmit` entry, base `main`) | no transfer because codex has no hook mechanism to wire it into — same reason as every other hook-based row above | no transfer, same reason as codex |
| 2026-09-07 | [`adc36bb`](https://github.com/alex137/BestPractice/commit/adc36bb3) — the private individual source's URL stops being baked into a file the consuming repo tracks: `individual-source-bootstrap.sh.template` reads `individual.repo_url` from the per-person `~/.config/precedent/config.json` first, and `gitignore.template` starts ignoring `.precedent/` | applied as `hooks/individual-source-bootstrap.sh.template` (config-first URL resolution, named-reason degradation when absent, baked default kept last) | no transfer because codex has no hook mechanism to wire it into — same reason as every other hook-based row above. Worth stating separately: that also means a codex user has no automated individual-source bootstrap at all, which is a pre-existing gap in the adapter, not something this change introduced | no transfer, same reason as codex |
| 2026-09-07 | [`fba1579`](https://github.com/alex137/BestPractice/commit/fba1579009c95505a29f0b79f246ecc0a361221a) — a person's declared timezone stops being a thing they retype: `commit-identity.sh` derives it into `.claude/settings.local.json`'s `env` block, and `gitignore.template` starts ignoring that file | applied as `hooks/commit-identity.sh` (the derivation, the never-write-a-guess rule, and the not-gitignored warning) and `gitignore.template` (the new ignore line) | no transfer because codex has no hook mechanism to wire it into — same reason as every other hook-based row above. The `env` block this derives into is Claude Code's own settings schema, so even a codex hook mechanism would need a different carrier | no transfer, same reason as codex |
| 2026-09-07 | [`5207720`](https://github.com/alex137/BestPractice/commit/520772036a0f9d88123eda7d24287048eb1c913b) — the commit identity is set GLOBALLY and a backstop is installed at `core.hooksPath`, because a repo attached mid-session inherits the container's global identity and no per-checkout hook can reach it | applied as `hooks/commit-identity.sh` (global identity for a DECLARED identity only, chained global backstop, never stealing an existing `core.hooksPath`) | no transfer because codex has no hook mechanism to wire it into — but note this row is the first where that gap has real cost: the global half needs no harness at all, since `git config --global` and `core.hooksPath` are git's own settings, so a codex user could get the protection from a one-off run of this script even without hooks | no transfer, same reason as codex |
| 2026-09-07 | [`59f1864`](https://github.com/alex137/BestPractice/commit/59f1864f0b3e46acbc0dbf8066e3351edbe6810e) — the declared timezone stops being enforced-after-the-fact: `commit-identity.sh` repoints the container's system zone, so the wrong offset is never produced rather than produced and refused | applied as `hooks/commit-identity.sh` (`_set_system_timezone`, declared zones only, a warn-and-name-the-fallback path when the clock file is unwritable, and `PRECEDENT_LOCALTIME` so the block is testable without moving the machine's own clock) | **partial transfer, and this row is the first where the missing half is worth spelling out.** Two mechanisms are inside this change, and they separate cleanly. The *decision* — resolve a declared zone, refuse to act on a guess — needs the hook mechanism codex does not have, same as every hook-based row above. The *action* is `ln -sf /usr/share/zoneinfo/<zone> /etc/localtime`, which needs no harness at all: it is the operating system's own setting, like `git config --global` in the row above it. So a codex user running this script once by hand gets the offset fix outright, and only loses the automatic re-application at each session start | no transfer, same reason as codex |
| 2026-09-10 | [`4abb73b`](https://github.com/alex137/BestPractice/commit/4abb73b583e0893d299e9d2a8af01d235979cbb9) — the engine's last-resort timezone stops being one person's zone: `DEFAULT_TZ` moves from `America/Argentina/Buenos_Aires` to `America/New_York`, so a PUBLIC generic engine no longer stamps an unidentified committer with the zone of the person who wrote it | applied as `hooks/commit-identity.sh` (`DEFAULT_TZ`, and the docstring paragraph that says the fallback is declared rather than guessed), kept byte-identical to the installed `.claude/hooks/` copy the harness's own copy check compares | no transfer because codex has no hook mechanism to wire it into — same reason as every other hook-based row above. Worth noting the value itself is not adapter-specific: it is declared in [precedent.json](../../precedent.json) at rung 5 and read by [tools/precedent_time.py](../../tools/precedent_time.py), so a codex user gets the corrected zone for everything the engine stamps into documents, and loses only the session-clock repointing the hook performs | no transfer, same reason as codex |
| 2026-09-10 | [`b0ab5ce`](https://github.com/alex137/BestPractice/commit/b0ab5ce0) — `precedent-paths.sh`: the comment explaining why the hook omits `permissionDecision` pointed a reader at `templates/nontechnical-document-project/AGENTS.md`, a path that stopped existing when [technical-describes-people](../../practices/technical-describes-people.md) renamed that directory. Repointed to `templates/document-project/AGENTS.md` | applied as `hooks/precedent-paths.sh`, byte-identical to `.claude/hooks/precedent-paths.sh` as the harness's own copy check requires; no wiring change — the same `PreToolUse` entry runs it. **The verdict is the whole row here: this is a comment, so nothing executes differently in any member.** It still transfers, because a stale path in a comment is what sends the next reader looking for a directory that is gone, and that cost is identical in every copy of the file | no transfer because codex has no hook mechanism to wire this hook into — the same reason as every other hook-based row above, and the comment travels only with the hook | no transfer, same reason as codex |
| 2026-09-11 | [`e572e9e`](https://github.com/alex137/BestPractice/commit/e572e9e599e540bdb41f195f28280d5e9f0ebfcf) — `freshness-guard.sh`: a branch origin has never heard of is not a stale one. `git fetch origin <branch>` exits non-zero both when origin is unreachable and when origin simply has no such ref, and `pre-write` treated the two identically — so it refused the first write of every newly created branch, naming a remedy (`git fetch origin <branch>`) that cannot succeed against a ref that does not exist, which left `git config precedent.freshness.override true` as the only way forward and switched the whole guard off. `git ls-remote --exit-code --heads origin <branch>` separates them; only exit 2 is waved through, and the base-branch check still runs on it | applied as `hooks/freshness-guard.sh`, byte-identical to `.claude/hooks/freshness-guard.sh`; no wiring change — the same `SessionStart`, `UserPromptSubmit` and `PreToolUse` entries run it. **The defect was shared exactly as widely as the mechanism**: every repo instantiated from this template carries it, and the fix reached this repo from a consuming one ([practice-export-loop](../../practices/practice-export-loop.md)) rather than originating here | no transfer because codex has no hook mechanism to wire the guard into — the same reason as every other hook-based row above. Worth stating for this row: the mechanism is not harness-specific at all — it is a shell script needing one invocation per session — so a codex user loses the guard entirely, not merely this fix to it | no transfer, same reason as codex |
| 2026-09-11 | `PENDING` — the freshness gate extends over repositories the session merely has ATTACHED, named by `PRECEDENT_FRESHNESS_ALSO` (`;`-separated `<path>=<base branch>`); carried up from a downstream set, which had grown the mechanism locally | applied as `hooks/freshness-guard.sh` — no `settings.json` change, since the also-list is read from the environment precisely because hook wiring is what cannot reach an attached repo | no transfer because codex has no hook mechanism to wire the guard into at all — the same reason as every other freshness-guard row above. Worth recording that this half is the first part of this gate that is NOT harness-specific: it is an environment variable any ported gate would read, so if codex ever gains a pre-tool hook this row transfers whole rather than partially | no transfer, same reason as codex |
